RADIO_ROOT = '/local/lsd-radio'
MP3CACHE_ROOT = '%s/cache' % RADIO_ROOT
ICES_PID = '%s/ices.pid' % RADIO_ROOT
ICES_MODULE_ROOT = '/usr/local/etc/modules/'
ICES_PIPE= '%s/ices_pipe' % RADIO_ROOT
SERVER_URL = 'vermelho:8000'
STATUS_PAGE = 'http://%s/status.xsl' % SERVER_URL
SCRATCH_ROOT = '/scratch/mp3'v

